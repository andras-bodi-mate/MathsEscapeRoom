[project]
name = "maths-escape-room"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "alembic>=1.18.4",
    "fastapi[standard]>=0.134.0",
    "sqlmodel>=0.0.37",
    "sty>=1.0.6",
]
